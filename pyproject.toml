[project]
name = "nyc-taxi-insights"
version = "0.1.0"
description = "Data engineering pipeline for NYC Taxi data"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "db-dtypes",
    "fastparquet",
    "google-cloud-bigquery",
    "mage-ai>=0.9.79",
    "numpy",
    "pandas<2.2",
    "pandas-gbq",
    "pyarrow",
]
